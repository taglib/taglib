language: cpp

sudo: false

os:
  - linux
  - osx

dist: trusty

compiler:
  - gcc
  - clang

addons:
  apt:
    packages:
      - zlib1g-dev

matrix:
  exclude:
    - os: osx
      compiler: gcc

script: cmake -DBUILD_TESTS=ON -DBUILD_EXAMPLES=ON -DBUILD_BINDINGS=ON . && make && make check
